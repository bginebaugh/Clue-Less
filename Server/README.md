# Running the server through Gradle

You must have Gradle installed on your machine. To run, simply do the following:

From the Server directory, `gradle run`

That's it!
Currently, the server will listen on port 11000.
